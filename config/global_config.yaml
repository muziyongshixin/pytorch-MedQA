data:
  dataset:
    train_path: data/MedQA/renwei_kexue_train_24000.json
    dev_path: data/MedQA/renwei_kexue_dev_1676.json
    test_path: data/MedQA/new_test_600.json
  dataset_h5: data/kexue_renwei_word2vec.h5  #保存预处理的数据，embedding，w2id等
  ignore_max_len: 200 # in train data, context token len > ignore_max_len will be dropped
  ignore_max_ques_ans_len: 100

  embedding_path: data/full_embedding.txt   # data/glove.840B.300d.zip
  vocabulary_path: data/vocabulary

  model_path: checkpoint/model-weight.pt
  checkpoint_path: checkpoint/checkpoint

model:
  encoder:
    word_embedding_size: 200

global:
  random_seed: 123
  model: SimpleSeaReader
   #match-lstm+ # 'match-lstm', 'match-lstm+', 'r-net' or 'base'. Note that 'base' model is customized by base_model.yaml

train:
  continue: False # 默认是不是继续上次训练
  batch_size: 400  #最好是5的倍数，便于计算准确率
  valid_batch_size: 400 #default 32
  epoch: 50
  enable_cuda: True

  optimizer: 'adam'  # adam, sgd, adamax, adadelta(default is adamax)
  learning_rate: 0.0001  # only for sgd
  clip_grad_norm: 5

test:
  batch_size: 32
  enable_cuda: True

